---
import Layout from '../../layouts/Layout.astro';
import { getTranslation } from '../../i18n/translations';

interface LinkItem {
  name: string;
  url: string;
  description?: string;
}

const generalLinks: LinkItem[] = [
  {
    name: 'Keybase',
    url: 'https://keybase.io/silasolla',
    description: 'PGP public key',
  },
  {
    name: 'Keyoxide',
    url: 'https://keyoxide.org/be521fe39c353503b8a241a6f520a0c7fbde155f',
    description: 'PGP public key (2)',
  },
];

const codeDevLinks: LinkItem[] = [
  {
    name: 'GitHub',
    url: 'https://github.com/silasolla',
    description: 'Clackety-clack',
  },
  {
    name: 'Gist',
    url: 'https://gist.github.com/silasolla',
    description: 'Clackety… clack',
  },
  {
    name: 'Zenn',
    url: 'https://zenn.dev/silasolla',
    description: 'For comments and browsing',
  },
  {
    name: 'DEV Community',
    url: 'https://dev.to/silasolla',
    description: 'For comments and browsing (2)',
  },
];

const workCertificationLinks: LinkItem[] = [
  {
    name: 'Wantedly',
    url: 'https://www.wantedly.com/id/silasolla',
    description: 'Work stuff',
  },
  {
    name: 'Credly',
    url: 'https://www.credly.com/users/silasolla',
    description: 'Certifications (mostly Google Cloud)',
  },
];

const socialMediaLinks: LinkItem[] = [
  {
    name: 'Bluesky',
    url: 'https://bsky.app/profile/silasol.la',
    description: 'Home base',
  },
  {
    name: 'Twitter (X)',
    url: 'https://twitter.com/silasolla',
    description: 'Internet arena',
  },
  {
    name: 'mixi2',
    url: 'https://mixi.social/@silasolla',
    description: 'Occasionally active',
  },
  {
    name: 'Misskey.io',
    url: 'https://misskey.io/@silasolla',
    description: "Haven't checked recently",
  },
  {
    name: 'Instagram',
    url: 'https://www.instagram.com/cosilasolla',
    description: 'Lurking',
  },
  {
    name: 'Threads',
    url: 'https://www.threads.net/cosilasolla',
    description: 'Check in sometimes',
  },
  {
    name: 'Reddit',
    url: 'https://www.reddit.com/u/silasolla',
    description: 'Made to look cool',
  },
  {
    name: 'SoundCloud',
    url: 'https://soundcloud.com/user-922982246-279093139',
    description: 'Just listening',
  },
];

const pageTitle = getTranslation(Astro.currentLocale, 'page.links');
const siteName = getTranslation(Astro.currentLocale, 'site.name');
---

<Layout
  title={`${pageTitle} - ${siteName}`}
  description="Links to various accounts of Masaki Haga (@silasolla)"
>
  <article class="links-page">
    <section class="links-section">
      <h2 class="section-title">General</h2>
      <div class="links-grid">
        {
          generalLinks.map((link) => (
            <a href={link.url} class="link-card" target="_blank" rel="noopener noreferrer">
              <h3 class="link-name">{link.name}</h3>
              {link.description && <p class="link-description">{link.description}</p>}
              <span class="link-arrow">→</span>
            </a>
          ))
        }
      </div>
    </section>

    <section class="links-section">
      <h2 class="section-title">Code / Dev</h2>
      <div class="links-grid">
        {
          codeDevLinks.map((link) => (
            <a href={link.url} class="link-card" target="_blank" rel="noopener noreferrer">
              <h3 class="link-name">{link.name}</h3>
              {link.description && <p class="link-description">{link.description}</p>}
              <span class="link-arrow">→</span>
            </a>
          ))
        }
      </div>
    </section>

    <section class="links-section">
      <h2 class="section-title">Work / Certifications</h2>
      <div class="links-grid">
        {
          workCertificationLinks.map((link) => (
            <a href={link.url} class="link-card" target="_blank" rel="noopener noreferrer">
              <h3 class="link-name">{link.name}</h3>
              {link.description && <p class="link-description">{link.description}</p>}
              <span class="link-arrow">→</span>
            </a>
          ))
        }
      </div>
    </section>

    <section class="links-section">
      <h2 class="section-title">Social Media</h2>
      <div class="links-grid">
        {
          socialMediaLinks.map((link) => (
            <a href={link.url} class="link-card" target="_blank" rel="noopener noreferrer">
              <h3 class="link-name">{link.name}</h3>
              {link.description && <p class="link-description">{link.description}</p>}
              <span class="link-arrow">→</span>
            </a>
          ))
        }
      </div>
    </section>

    <section class="links-note">
      <p>
        * Accounts not listed here (except those I’ve shared with you directly, either in person or
        via DM) may be impersonations, so please exercise caution.
      </p>
      <p>
        * If you’re unsure, feel free to contact me via DM on any social media platform or by email
        at me[at]silasol.la.
      </p>
    </section>
  </article>
</Layout>

<style>
  .links-page {
    padding: 2rem 0;
  }

  .links-section {
    margin-bottom: 3rem;
  }

  .section-title {
    font-size: 1.6rem;
    color: var(--color-primary-dark);
    margin-bottom: 1.5rem;
    padding-bottom: 0.5rem;
    border-bottom: 2px solid var(--color-primary);
  }

  .links-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 1.5rem;
  }

  .link-card {
    position: relative;
    display: block;
    background: var(--color-bg-alt);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    padding: 1.5rem;
    text-decoration: none;
    color: inherit;
    transition: all 0.2s ease;
  }

  .link-card:hover {
    background: var(--color-bg);
    border-color: var(--color-primary);
    box-shadow: 0 4px 12px rgba(90, 138, 94, 0.2);
    transform: translateY(-2px);
  }

  .link-name {
    font-size: 1.2rem;
    color: var(--color-primary-dark);
    margin-bottom: 0.5rem;
    padding-right: 2rem;
  }

  .link-description {
    color: var(--color-text-light);
    font-size: 0.9rem;
    margin: 0;
    line-height: 1.5;
  }

  .link-arrow {
    position: absolute;
    top: 1.5rem;
    right: 1.5rem;
    font-size: 1.5rem;
    color: var(--color-primary);
    transition: transform 0.2s ease;
  }

  .link-card:hover .link-arrow {
    transform: translateX(4px);
  }

  .links-note {
    background: var(--color-primary-light);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    padding: 1rem;
    margin-top: 3rem;
  }

  .links-note p {
    margin: 0;
    color: var(--color-text-light);
    font-size: 0.9rem;
    text-align: center;
  }

  @media (max-width: 640px) {
    .section-title {
      font-size: 1.5rem;
    }

    .links-grid {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .link-card {
      padding: 1.25rem;
    }

    .link-name {
      font-size: 1.1rem;
    }
  }
</style>

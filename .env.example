# サイトの URL (本番環境)
# RSS 生成, サイトマップ, OGP, Canonical URL
SITE_URL=https://example.com

# 画像を取得する URL (GitHub Actions などで設定してください)
# 未設定だとローカルの /public/posts/ を見ます
# IMAGE_BASE_URL=https://image.example.com

# Cloudflare R2 のバケット名 (開発時のみ使用)
R2_BUCKET_NAME=public

# Option: 開発時の ngrok ホスト (開発時のみ使用)
# NGROK_HOST=your-ngrok-id.ngrok-free.app
